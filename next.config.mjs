/** @type {import('next').NextConfig} */
const nextConfig = {
  devIndicators: { enable: true },
  // output: "export",
  trailingSlash: true,
  basePath: "/kasiro_start_page",
  assetPrefix: "/kasiro_start_page/",
};

export default nextConfig;
